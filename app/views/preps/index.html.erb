<div class="container-fluid bg-light d-flex flex-column" style="min-height: 100vh">
  <div class="container mb-auto">
  
    <h1 class="mt-4">現実と夢を記述する</h1>

    <div class="col-md-12 ">
      <div class="login-or">
          <hr class="hr-or">
      </div>
    </div>

     <p>未来を語る前に、今の現実を知らなければならない<br>
    現実からしかスタートできないからである。<br>by P.F.ドラッカー</p>

    <div class="col-md-12 ">
      <div class="login-or">
          <hr class="hr-or">
      </div>
    </div>

      <%= form_with model: @prep, url:new_prep_path,id: 'prep-form', class: 'prep-form-wrap',local: true do |f| %>

      <%# <%= render 'shared/error_messages' %> 

        <div class="form-group">
          <label for="formGroupExampleInput">ゴールを目指している今の気持ちを書いてみる</label>
          <input type="text" class="form-control" autofocus: true id="formGroupExampleInput" placeholder="(例)なんかモヤモヤしている">
        </div>
        <div class="form-group">
          <label for="formGroupExampleInput2">ゴールに到達した時の”わたし”の気持ちを書いてみる</label>
          <input type="text" class="form-control" id="formGroupExampleInput2" placeholder="(例)なんかかいい感じ">
        </div>

        <div class="text-center mb-5">
          <%= f.submit "記録する" ,class:"btn btn-block send-button tx-tfm" %>
        </div>
      <% end %>

      <div class="form_tag"> 
        <div class="col-md-12 ">
          <div class="login-or">
            <hr class="hr-or">
          </div>
        </div>
         
        <div class="prep_list">
          <ul class="preps_lists">
            <% if @preps %>
              <% @preps.each do |prep|%>
                <li class="preps_list">
                  <%= prep.now %>
                  <%= prep.target %>
                </li>
              <% end%>
            <% end %>
          </ul>
        </div>
  </div>
</div>